# Hostinger Deploy Notes

- Check PHP version
- Confirm `pdo_mysql` enabled
- Confirm `mod_rewrite`
- Ensure `uploads/anuncios/` is writable
- Update DB credentials in `includes/db.php`
- Import `database/schema.sql`
