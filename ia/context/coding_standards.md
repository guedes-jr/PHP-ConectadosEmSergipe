# Coding Standards

- PHP 8+ with `declare(strict_types=1)` when practical
- PDO with prepared statements only
- Escape output with `htmlspecialchars`
- Small helper functions preferred over complex abstractions
- Mobile-first CSS
- Minimal JS
- Keep files focused
