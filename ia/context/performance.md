# Performance Playbook

- Avoid unnecessary abstractions
- Keep HTML server-rendered
- Use lazy loading for public images
- Select only needed columns
- Avoid N+1 query patterns in public listings
- Limit admin listing volume
- Keep CSS and JS tiny
