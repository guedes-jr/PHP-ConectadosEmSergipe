# Definition of Done

A module is done only when:
- code runs without fatal errors
- output is escaped where needed
- queries use prepared statements where input exists
- layout remains responsive
- no unnecessary files were changed
- README or notes updated when behavior changes
